const formUl = document.querySelector(".feedback-form");
const textarea = document.querySelector("textarea");


// formUl.addEventListener("input", formSubmitHandler)

function formSubmitHandler(event) {
    const text = textarea.value;
}